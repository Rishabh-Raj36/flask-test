# 1. Dependencies
# Never upload these; they are reinstalled on the server
node_modules/
jspm_packages/
package-lock.json

# 2. Environment Variables & Secrets
# Crucial: Protect your API keys and database credentials
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.pem

# 3. OS & Editor files
# Prevents system-specific junk from cluttering the repo
.DS_Store
Thumbs.db
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln

# 4. Logs
# Prevents huge log files from being tracked
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/
*.log

# 5. Build/Dist folders
# If you are using TypeScript or a build step
dist/
out/
build/